repos:
  - repo: local
    hooks:
      - id: forbid-snake-case-filenames
        name: Forbid snake_case filenames
        entry: bash -c 'if git diff --cached --name-only | grep -E "_"; then echo "❌ Snake_case filenames are not allowed!"; exit 1; fi'
        language: system
        types: [file]
